{
  "name": "eslint-boundary-rules-pack",
  "version": "1.0.0",
  "private": true,
  "description": "Custom ESLint rules that turn architectural conventions into rules which fail CI: layer order, feature isolation, public entry points, hermetic tests, no debug output in production, barrel cycles, and a declarative dependency-direction engine.",
  "type": "commonjs",
  "license": "SEE LICENSE IN LICENCE.txt",
  "engines": {
    "node": ">=20.19.0"
  },
  "scripts": {
    "test": "node --test \"tests/*.test.js\"",
    "lint:fixtures": "eslint fixtures",
    "lint:fixtures:fix": "eslint fixtures --fix"
  },
  "peerDependencies": {
    "eslint": "^9.0.0 || ^10.0.0"
  },
  "devDependencies": {
    "@typescript-eslint/parser": "^8.70.0",
    "eslint": "^10.10.0"
  },
  "keywords": [
    "eslint",
    "eslintplugin",
    "architecture",
    "boundaries",
    "layers",
    "flat-config"
  ]
}
